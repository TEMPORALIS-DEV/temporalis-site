import Section from "../../layout/Section";
import Panel from "../../ui/Panel";

export default function GovernancePositioning() {
  return (
    <Section>
      <Panel className="p-8">
        <h2 className="text-2xl font-medium tracking-tight">
          Governance as a capital constraint, not a narrative tool.
        </h2>
        <p className="mt-4 text-sm leading-relaxed opacity-70">
          TEMPORALIS does not promise upside. It enforces discipline.
          It defines when exposure increases, compresses, or remains stable —
          based on measured behavior.
        </p>
      </Panel>
    </Section>
  );
}